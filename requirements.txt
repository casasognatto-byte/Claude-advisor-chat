fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
python-dotenv>=1.0
itsdangerous>=2.0
psycopg2-binary>=2.9
passlib[bcrypt]>=1.7.4,<2
bcrypt>=4.0,<4.1  # passlib 1.7.4 quebra com bcrypt>=4.1 (remove bcrypt.__about__)
tzdata>=2024.1  # zoneinfo precisa disso no Windows (sem banco de fuso do IANA nativo)
httpx>=0.27  # chamadas assíncronas ao fornecedor de imagem (Nano Banana) e ao ClickUp
python-multipart>=0.0.9  # necessário pro FastAPI aceitar upload de imagem (UploadFile/Form)
boto3>=1.34  # cliente S3 usado pra falar com Cloudflare R2 (Biblioteca de Apresentações)
openpyxl>=3.1  # lê a planilha .xlsx de cores Simonetto/Stimmo (importação no painel admin)
