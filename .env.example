# Copie para .env e preencha. NUNCA versione o .env real.

# Sem isso, print() de diagnóstico ficam presos no buffer e não aparecem no
# log a tempo (ou nunca aparecem). Sempre deixe ligado, local e em produção.
PYTHONUNBUFFERED=1

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# Opcional — system prompt do executor. Coloque aqui o contexto do Promob, ex.:
# SYSTEM_PROMPT=Você é um assistente especializado no Promob...
SYSTEM_PROMPT=

# --- Login / autenticação ---------------------------------------------------
# Login é por E-MAIL. O quadro fixo de usuários (ROSTER, em app/main.py) é
# aplicado a cada boot: o diretor (Davi) entra confirmado com a senha abaixo;
# os demais entram PENDENTES e recebem um e-mail de convite para confirmar o
# cadastro e definir a própria senha. Gestão adicional pelo painel /admin.

# Senha inicial do diretor (Davi Nogueira). Definida só se ele ainda não tiver
# senha; depois disso ele troca pela própria. Sem isso, nem o diretor loga.
SEED_DIRETOR_PASSWORD=

# --- E-mail (convite de cadastro + redefinição de senha) --------------------
# EMAIL_BACKEND: "console" (padrão) só ESCREVE o link no log, não envia nada —
# use em dev/teste. "smtp" envia de verdade. Nenhum e-mail real sai enquanto
# estiver em "console".
EMAIL_BACKEND=console
# Remetente exibido nos e-mails.
EMAIL_FROM=Casa Sognatto <casasognatto@gmail.com>
# Só quando EMAIL_BACKEND=smtp — credenciais do Gmail de uso da empresa
# (casasognatto@gmail.com). SMTP_PASSWORD precisa ser uma "Senha de app" do
# Gmail (myaccount.google.com/apppasswords, exige verificação em 2 etapas
# ativada) — NUNCA a senha normal da conta Google. Porta 465=SSL, 587=STARTTLS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=casasognatto@gmail.com
SMTP_PASSWORD=
# SMTP_STARTTLS=true

# Todo e-mail de "esqueci minha senha" vai em cópia oculta (BCC) pra essa caixa
# também — assim o diretor sempre tem uma via de acesso pra completar a
# redefinição de senha de qualquer usuário (ex: alguém desligado da empresa),
# sem depender do e-mail pessoal dessa pessoa. Já é o padrão no código mesmo
# sem definir isso aqui.
# PASSWORD_RESET_BCC=casasognatto@gmail.com

# Chave para assinar o cookie de sessão. Gere uma aleatória e mantenha fixa
# (se mudar, todos precisam logar de novo). Ex.: um texto longo e aleatório.
SECRET_KEY=

# Em produção (HTTPS) deixe true. Só use false para testar local em http://.
COOKIE_SECURE=true

# URL pública deste serviço (ex: https://chat.casasognatto.com.br) — usada nos
# links de convite/redefinição de senha enviados por e-mail.
PUBLIC_BASE_URL=http://127.0.0.1:8000

# --- Banco de dados (login + histórico compartilhado da equipe) -------------
# String de conexão Postgres (Neon, Supabase, Render...). AGORA OBRIGATÓRIO —
# sem isso ninguém consegue logar (não há mais fallback "site aberto"). Ex.:
# DATABASE_URL=postgresql://user:senha@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# --- Alerta ClickUp (pergunta sobre o stack de IA) ---------------------------
# Se qualquer pessoa perguntar qual IA/tecnologia está por trás do Sogno, o
# sistema detecta e manda uma mensagem de chat direta (DM) pro Davi no
# ClickUp. Sem CLICKUP_TOKEN, só loga no servidor (não quebra o chat).
# Testado de ponta a ponta em 2026-07-04 com token real.
CLICKUP_TOKEN=
# CLICKUP_WORKSPACE_ID=90133031055           # já é o padrão no código
# CLICKUP_ALERT_EMAIL=davinogueira@casasognatto.com.br  # já é o padrão no código

# --- Opcionais (têm padrões no código) --------------------------------------
# EXECUTOR_MODEL=claude-sonnet-4-6
# ADVISOR_MODEL=claude-opus-4-8
# MAX_TOKENS=4096
# ADVISOR_MAX_USES=3
# ADVISOR_MAX_TOKENS=2048
# SESSION_MAX_AGE=43200

# --- Geração de imagem (imagem elementar -> render fotorrealista) -----------
# IMPORTANTE (ver memória "Confidencialidade do stack de IA"): nunca expor o
# nome de fornecedor em nada visível às arquitetas — isso já é garantido no
# código, só documentando aqui pra quem for mexer no futuro.

# Motor padrão: stub | nanobanana | redraw.
# "stub" não chama fornecedor nenhum — só simula, útil pra testar sem gastar.
IMAGE_ENGINE=stub

# --- Nano Banana (Gemini API) ------------------------------------------------
# Chave do fornecedor (Nano Banana é via Gemini API) — só preencha quando for
# usar de verdade.
GOOGLE_API_KEY=
# IMAGE_MODEL (opcional, padrão no código: gemini-3-pro-image = Nano Banana
# Pro, melhor fidelidade a materiais/marcas específicas). Alternativa mais
# barata: gemini-3.1-flash-image (Nano Banana 2). NUNCA usar
# gemini-2.5-flash-image (Nano Banana original) — testado em produção
# (2026-07-07) e não seguiu bem nomes de material como "Suvinil crômio" e
# "quartzito Taj Mahal".

# --- ReDraw AI ---------------------------------------------------------------
# ATENÇÃO: a API pública oficial do ReDraw AI não foi localizada no momento
# da implementação. O engine abaixo usa um contrato REST genérico; o endpoint
# e o formato da resposta podem precisar de ajustes quando o Davi fornecer a
# documentação ou a API key real do fornecedor.
REDRAW_API_KEY=
REDRAW_API_URL=https://api.redraw.ai/v1/redraw
REDRAW_TIMEOUT=90

# Outro opcional (tem padrão no código): IMAGE_STORAGE_DIR

# --- Biblioteca de Apresentações (Fase 4: Sogno arquiteta auxiliar) ---------
# Armazenamento das imagens/vídeos de cada projeto de cliente. Sem as 4
# variáveis abaixo, cai automaticamente pra disco local (dev/teste, sem
# custo, mas não persiste entre deploys). Cloudflare R2 recomendado: sem
# custo de egress, $0,015/GB/mês, grátis até 10GB.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Opcional (só usado no fallback local, sem R2 configurado):
# PRESENTATIONS_STORAGE_DIR=

# --- Filmmaker (producao de video com Creatomate) ---------------------------
# Motor: stub | creatomate. "stub" nao chama API externa — util pra testar.
FILMMAKER_ENGINE=stub
# Chave da API Creatomate (Essential plan, $54/mes). Sem ela, modo stub.
CREATOMATE_API_KEY=
# CREATOMATE_TIMEOUT=300

# --- Sogno Code (módulo de desenvolvimento, só o Davi) ----------------------
# Chave da API Moonshot pro chat de desenvolvimento (Kimi). Sem ela, o módulo
# responde 500 explicando e o resto do app (Chat) funciona normal.
MOONSHOT_API_KEY=
# MOONSHOT_BASE_URL=https://api.moonshot.ai/v1   # já é o padrão no código
# CODE_DEFAULT_MODEL=kimi-k3                     # padrão; seletor na tela
# CODE_MAX_TOKENS=8192
# E-mail do único usuário com acesso ao Code (já é o padrão no código):
# CODE_MASTER_EMAIL=davinogueira@casasognatto.com.br

# Modelos Claude no Sogno Code (opcional). Se CODE_ANTHROPIC_API_KEY ou
# ANTHROPIC_API_KEY estiver configurada, o Sogno Code ganha um botão Claude
# ao lado do Kimi. Sem chave Anthropic, só o Kimi aparece.
# CODE_ANTHROPIC_API_KEY=
# CODE_CLAUDE_DEFAULT_MODEL=claude-sonnet-4-6
# CODE_CLAUDE_MAX_TOKENS=4096

# --- Motor Kimi no Chat principal (opcional) --------------------------------
# Se quiser oferecer Kimi como motor alternativo ao Claude no chat das
# arquitetas, configure uma dessas duas variáveis. KIMI_CHAT_API_KEY tem
# prioridade; se não existir, o código reusa MOONSHOT_API_KEY do Sogno Code.
# KIMI_CHAT_API_KEY=
# KIMI_CHAT_MODEL=kimi-k3                        # padrão
# KIMI_CHAT_MAX_TOKENS=4096                      # padrão
# KIMI_CHAT_BASE_URL=https://api.moonshot.ai/v1  # padrão

# Chave Anthropic separada, criada pelo Davi (26/07/2026) pra futura integração
# Kimi + Claude no Sogno Code (seletor com modelos Claude — fase posterior).
# NÃO é usada hoje; o Chat continua na ANTHROPIC_API_KEY de cima.
CODE_ANTHROPIC_API_KEY=
